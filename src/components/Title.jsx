import React from 'react'

const Title = ({title}) => {
  return (
    <div>
        <div className="bg-slate-100 px-3 py-2 rounded w-full m-1 mt-5">{title}</div>
    </div>
  )
}

export default Title